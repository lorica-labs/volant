#!/bin/sh
head -1 "$1" | grep -Eq '^(feat|fix|docs|ci|refactor|test|chore|perf|build)(\([a-z0-9-]+\))?!?: [a-z]' \
  || { echo "commit subject must follow Conventional Commits, e.g. 'feat(agent): add facts collector'" >&2; exit 1; }
