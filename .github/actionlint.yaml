# release.yml is generated by dist and regenerated on every `dist init`, so hand edits are lost
# and would make `dist generate --check` fail. Its shell steps are not ours to correct.
paths:
  .github/workflows/release.yml:
    ignore:
      - ".*"
