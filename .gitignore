/target/
/docs/node_modules/
/docs/dist/
/docs/.astro/
/mutants.out*

# editor project files, wherever they show up in the tree
**/.idea/

# planning notes and local assistant files stay out of the repository
/docs/superpowers/
/.superpowers/
/.claude/*
!/.claude/skills/

# the controller Python the ssh job builds at the checkout root
/.venv/

# the collections the ssh job installs beside it
/.collections/

# the tree's own `git describe`, written by the `remote` recipe for `bench`/`bench-k3s` to read
# on a copy that has no `.git` of its own
/.volant-describe
