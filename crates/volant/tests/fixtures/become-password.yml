- hosts: localhost
  gather_facts: false
  become: true
  tasks:
    - name: Escalated
      command: echo escalated
      register: out
    - name: Say it
      debug:
        msg: "{{ out.stdout }}"
