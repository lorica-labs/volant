- hosts: h1,h2
  gather_facts: false
  tasks:
    - command: "false"
      when: inventory_hostname == 'h1'
- hosts: h1
  gather_facts: false
  tasks:
    - debug: msg=second-h1-only
- hosts: h2
  gather_facts: false
  tasks:
    - debug: msg=third
