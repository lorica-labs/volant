- name: A vars_files entry that renders to a number
  hosts: web
  gather_facts: false
  vars_files:
    - "{{ 1 + 1 }}"
  tasks:
    - name: Never reached
      debug:
        msg: "reached on {{ inventory_hostname }}"
