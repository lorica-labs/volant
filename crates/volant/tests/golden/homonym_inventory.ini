# Fixture for the golden's dedicated homonym check: a group and one of its own hosts share a
# name. Kept separate from inventory.ini so that fixture's sixteen patterns can genuinely test
# "the reference does not warn here" instead of every one of them warning unconditionally.
[same]
same
other
