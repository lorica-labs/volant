# Empty on purpose. Both the recording and the test run with this directory as the working
# directory, and an ansible.cfg here is the first file ansible-playbook and volant look at, so
# it shadows whatever the machine keeps in ~/.ansible.cfg or /etc/ansible/ansible.cfg. Without
# it the recorded bytes would depend on the machine that recorded them.
[defaults]
