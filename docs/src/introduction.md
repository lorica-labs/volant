# Introduction

Volant is a fast, drop-in engine for Ansible playbooks. It runs your playbooks, roles, collections and inventories as they are, over SSH to Linux hosts. It uploads a small agent once per host and sends tasks to it in batches.

The project is in pre-alpha, and so is this site. It will cover installation, a compatibility reference, the plan command, and the architecture decisions that affect users.
